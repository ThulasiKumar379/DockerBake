# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy custom HTML file to the Nginx server's default HTML directory
COPY index.html /usr/share/nginx/html/

# Expose port 80
EXPOSE 80

# Use the default command provided by the Nginx image

